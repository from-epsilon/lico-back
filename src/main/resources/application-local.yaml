spring:
  jpa:
    hibernate:
      ddl-auto: update

  flyway:
    enabled: false
    clean-disabled: false

management:
  endpoints:
    web:
      exposure:
        include:
          - health
  endpoint:
    health:
      show-details: ALWAYS
